allow-rules:
  # autobahn ships a demo private key in cryptosign.py for its WAMP
  # cryptographic signing feature. It is not a real secret.
  - id: ignore-autobahn-demo-key
    message-regex: ".*"
    path-regex: ".*(autobahn/wamp).*"
